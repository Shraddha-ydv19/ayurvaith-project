Place your logo file at `assets/logo.png`.
If you add a custom pattern or illustration, update the CSS to point to it.
Stock images currently load from royalty-free Unsplash URLs; you can swap them with local files in this folder and adjust the image `src` paths in `index.html`.

